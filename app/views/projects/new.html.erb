<div class="container">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                    <br> <br> <br> <br> <br>
                    <h1>New project</h1>

                    <% if flash[:error] %>
                      <p><%= flash[:error] %></p>
                    <% end %>

                    <%= form_for @project do |f| %>
                    <fieldset>
                    <div>
                        <%= f.label :title %>
                        <%= f.text_field :title %>
                    </div>
                    <div>
                        <%= f.label :brief %>
                        <%= f.text_field :brief%>
                    </div>
                    <div>
                        <%= f.label :description %>
                        <%= f.text_field :description%>
                    </div>

                    <div>
                        <%= f.label :image1 %>
                        <%= f.text_area :image1%>
                    </div>
                    </fieldset>
                    <div> <%=f.submit "Add", :class => 'btn btn-default btn-lg'%>
                    <%end%>

                    </div>
                </div>
            </div>
        </div>
